// Define data type as you pass in an agrument, here we set the parameters to a 'number' type

function add(num1: number, num2: number): number {
  // We can assign 'types' to a variable. The 'any' type, we can put strings,number, booleans
  const example: any[] = ["", 0, true];

  return num1 + num2;
}
